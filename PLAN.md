# kin-radar — 지식iN 수요 기반 지식 콘텐츠 파이프라인

지식iN의 인기 질문을 수요 신호로 삼아, 독자적인 지식 콘텐츠를 만들어 광고 수익을 내는 파이프라인.

## 핵심 원칙 (안전선)

- 지식iN 수집은 **네이버 공식 검색 API만** 사용 (크롤링 금지)
- 수집한 원문(제목·요약)은 **내부 분석 전용** — 사이트/블로그에 절대 게시하지 않음
- 콘텐츠 생성 시 지식iN 원문을 프롬프트에 넣지 않음 — 주제와 질문 의도만 입력 (저작권 안전선)
- 발행은 초안까지만 자동, 최종 발행은 검수 후 수동

## 방향 결정 (2026-07-29)

- **발행처·수익: B안 확정 — 자체 사이트 + 애드센스** (실질 유입은 구글 검색)
- **니치: 종합 생활정보 (사용자 결정, 2026-07-29)** — 분야를 좁히지 않고 질문 많은 주제 전반을 다루되, "생활 궁금증 해결" 우산 컨셉 + 명확한 카테고리 구조로 잡블로그로 보이지 않게 구성. 운영하며 Search Console 데이터로 순위 붙는 분야에 집중 투자 (데이터가 니치를 정하게)
- 네이버 검색 노출은 서치어드바이저 등록으로 보조 채널 취급 (기대치 낮게)

## 리소스

- 네이버 검색 API (지식iN): 일 25,000회, `.env`의 NAVER_CLIENT_ID/SECRET
  - 개발자센터 키는 2027-06-30까지 유효, 이후 NAVER API HUB 이관 필요
- 네이버 데이터랩 검색어트렌드: 일 1,000회 (호출당 키워드그룹 5개)
- Gemini API: `gemini-3.1-flash-lite` 500회/일 (대량 1차 분류), `gemini-3.5-flash-lite` 500회/일 (라벨링·요약)
  - 배치 전략: 호출 1회에 질문 50~100개 묶음 처리
- NCP 가입 + API HUB 신청: 2026-09-30 전 가입 시 크레딧 20만 원 (미완)
- 네이버 검색광고 계정 (키워드 도구, 검색량 확인용): 미가입

## 마일스톤

### M1: 수집기 (1~2일)

- 시드 키워드 목록 → `kin.json`을 sort=sim(볼륨) + sort=point(품질) + sort=date(신규)로 페이징 수집
- SQLite 저장: 질문 제목·요약·링크·수집일·검색키워드
- 중복 제거(docId 기준), 일일 증분 모드

### M2: 주제 발굴 (2~3일)

- 수집된 질문을 Gemini로 클러스터링 → "같은 걸 묻는 질문 묶음" 생성
- 랭킹 점수 = 클러스터 크기(반복 빈도) + 최근성 가중치
- 출력: 주제 후보 테이블 (주제명, 대표 질문 유형, 빈도, 추세)
- 검토: gemini-embedding-2 쿼터 확인 → 임베딩 클러스터링 + LLM 라벨링 구조면 호출 절약

### M3: 검증기 (1~2일)

- 데이터랩 API로 후보 키워드의 검색 추세 자동 조회 (상승/유지/하락)
- 검색광고 키워드도구로 월간 검색수 확인 — 초기엔 수동, 나중에 검색광고 API로 자동화
- 타겟 검색엔진에서 해당 키워드 검색해 상위 노출 경쟁 강도 판정
- 최종 스코어 = 수요 × 추세 × (1/경쟁도) → 발행 큐에 적재

### M4: 콘텐츠 생성기 + 발행 큐 (2~3일)

- 입력은 주제와 질문 의도만 — 지식iN 원문은 프롬프트에 넣지 않음
- 한 주제를 완결성 있게 다루는 롱폼 (2,000자+, 목차·FAQ 구조)
- 생성 파이프라인: 1호출 vs 3호출(개요→본문→자가검토) 비교 테스트 후 확정
- **생성과 공개 분리**: 생성물은 발행 큐(DB status)에 적재, 매일 소량씩만 공개
- 확실하지 않은 수치·수수료·법조항은 단정 금지, 공식 확인처 안내 (팩트 안전선)

### 발행 전략 (2026-07-29 확정)

- 사이트 먼저 오픈 (검수 거친 50~100편) → 하루 10~20편 점진 발행
- 3~6개월: Search Console에서 색인·순위 붙는 분야 확인 → 집중 증량 (하루 30편+)
- 1년 내 5천~1만 편 도달 목표 — 신규 도메인에 대량 일괄 공개는 스팸 판정 위험이라 금지

### M5: 사이트 (2~3일)

- Astro + Cloudflare Pages(무료) + 커스텀 도메인(연 1~2만 원) — 글은 마크다운 커밋으로 자동 배포
- sitemap, 구조화 데이터(FAQ 스키마), Google Search Console + 네이버 서치어드바이저 등록

### M6: 운영 전환

- 일일 배치: 증분 수집 → 후보 갱신 → 초안 생성 → 검수 알림
- 수익 신청 기준: 글 30~50편 축적 후
- 첫 3~6개월 저트래픽은 정상 — 배치는 계속 돌면서 콘텐츠 자산 축적

## 관련 프로젝트

- `~/개인프로젝트/celeb_issue_auto/` — 연예이슈 블로그 파이프라인. 네이버 블로그 임시저장 발행 인프라 보유
